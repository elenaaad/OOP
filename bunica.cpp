#include<iostream>
#include<cstring>
using namespace std;

void ADD()
{
    cin>>forma;
    if (forma == cerc)
    {
        cin>>raza>>h;
        ariac = ariacerc()
        gem = ariac * h;
        pcerc= (2 * 3,14 * h);
        frisca = (2*ariac) + (p*h)
    }
    if (forma == triunghi)
        triunghi()
}

int ariacerc()
{
    float ariac;
    ariac = (3,14*raza*raza);
    return ariac;
}
int main()
{
    char cerc = 'cerc';
    char triunghi = 'triunghi';
    char patrat = 'patrat';
    char tri_drept = 'triunghi dreptunghic'
    char forma[20];
    int h, raza, latura, cat1, cat2, lmica, lmare;
    int gem, frisca;
}
